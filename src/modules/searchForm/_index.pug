include ../button/_index

mixin searchForm(isClass)
  form(class = "search "+ isClass)
    input.search__input(type = "search" placeholder = "Search" tabindex = "1")
    +button("search__btn", 'Search', 2)

